---
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import SEOHead from "@/components/SEOHead.astro";

interface Props {
  title: string;
  description?: string;
  canonical?: string;
  image?: string;
  type?: "website" | "product" | "article";
  noindex?: boolean;
  product?: {
    name: string;
    description: string;
    sku: string;
    price: number;
    currency: string;
    availability: "InStock" | "OutOfStock" | "PreOrder";
    brand?: string;
    category?: string;
    images?: string[];
    rating?: { value: number; count: number };
  };
  breadcrumbs?: Array<{ name: string; url: string }>;
}

const {
  title,
  description = "Ihr Online-Shop fuer hochwertige Produkte",
  canonical,
  image,
  type = "website",
  noindex = false,
  product,
  breadcrumbs,
} = Astro.props;
---

<!doctype html>
<html lang="de">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />

    <SEOHead
      title={`${title} - Skibidoo Shop`}
      description={description}
      canonical={canonical}
      image={image}
      type={type}
      noindex={noindex}
      product={product}
      breadcrumbs={breadcrumbs}
    />

    <!-- Preconnect to external resources -->
    <link rel="preconnect" href="https://unpkg.com" />
    <link rel="dns-prefetch" href="https://unpkg.com" />

    <!-- HTMX -->
    <script src="https://unpkg.com/htmx.org@2.0.4" defer></script>
  </head>
  <body class="flex min-h-screen flex-col bg-gray-50" hx-boost="true">
    <Header />
    <main class="flex-1">
      <slot />
    </main>
    <Footer />

    <div id="toast-container" class="fixed bottom-4 right-4 z-50 space-y-2"></div>

    <script is:inline>
      document.body.addEventListener("htmx:afterSwap", function(evt) {
        if (evt.detail.target.id === "cart-count") {
          evt.detail.target.classList.add("animate-bounce");
          setTimeout(() => evt.detail.target.classList.remove("animate-bounce"), 500);
        }
      });

      function showToast(message, type = "success") {
        const container = document.getElementById("toast-container");
        const toast = document.createElement("div");
        toast.className = "rounded-lg px-4 py-3 shadow-lg text-white " + (type === "success" ? "bg-green-500" : "bg-red-500");
        toast.textContent = message;
        container.appendChild(toast);
        setTimeout(() => toast.remove(), 3000);
      }

      document.body.addEventListener("showToast", function(evt) {
        showToast(evt.detail.message, evt.detail.type);
      });
    </script>
  </body>
</html>
